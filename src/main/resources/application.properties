# spring.profiles.active=${SPRING_PROFILES_ACTIVE:local}
spring.application.name=Medicina

# PostgreSQL REAL (Render)
# spring.datasource.url=${SPRING_DATASOURCE_URL:${DATABASE_URL:jdbc:postgresql://localhost:5432/medicinas_db}}
# spring.datasource.username=${SPRING_DATASOURCE_USERNAME:${DATABASE_USERNAME:postgres}}
# spring.datasource.password=${SPRING_DATASOURCE_PASSWORD:${DATABASE_PASSWORD:}}
# spring.datasource.driver-class-name=org.postgresql.Driver

# JPA PostgreSQL
# spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect
# spring.jpa.hibernate.ddl-auto=update
# spring.jpa.show-sql=${SHOW_SQL:true}
# spring.jpa.properties.hibernate.jdbc.lob.non_contextual_creation=true

# Server
server.port=${PORT:8080}

# Logging
logging.level.org.hibernate.SQL=${HIBERNATE_SQL_LOG:DEBUG}